Template.imgUpload.events({
/*	'change #qupload': function(event, template){
		alert("jqupload change");
		readURL(event.currentTarget);
		Uploader.startUpload.call(Template.instance(), event);
	} */
});